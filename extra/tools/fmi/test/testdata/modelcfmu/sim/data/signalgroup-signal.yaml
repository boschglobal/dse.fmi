---
kind: SignalGroup
metadata:
  labels:
    channel: signal_vector
    runnable: signal
  name: signal
spec:
  signals:
    - annotations:
        direction: input
      signal: reset_counters
    - annotations:
        direction: output
      signal: task_init_done
    - annotations:
        direction: output
      signal: task_5_active
